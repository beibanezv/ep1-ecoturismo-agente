[project]
name = "ep1-ecoturismo-agente"
version = "0.1.0"
description = "EP1 ISY0101 - Agente RAG que genera y replanifica itinerarios de ecoturismo ante clima y estado de senderos"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "groq>=0.11.0",
    "chromadb>=1.1.0",
    "sentence-transformers>=3.0.0",
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
    "pytest>=7.0.0",
]

[tool.uv]
package = false
